import numpy as np
from numpy import random

# arr = random.randint(1,10,(4,4))
# arr_sum_rows = np.asarray(arr).sum(axis=1)#Суммы строк матрицы
# sum_min = arr_sum_rows.min() # минимальная сумма
# index_minsum = list(arr_sum_rows).index(sum_min)#индекс минимальной суммы массива
# min_sum_row = arr[index_minsum]# строка с наименошей суммой

# print("Matrica\n",arr ,"\n")
# # print("Строка с минимальной суммой",min_sum_row)

# arr = np.delete(arr,-1,axis=1)

# print("Matrica\n",arr ,"\n")

